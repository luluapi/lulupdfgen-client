Pre-requisites:
Java 1.5 or above has been installed and JAVA_HOME is set with JAVA_HOME/bin in path

To run the examples in this package, first ensure all scripts have execute permissions.

Then run clean-compile.sh to clean classes directory and compile java files in src directory.
./clean-compile.sh 

Then run the example scripts.
